//! Schema inheritance and merge logic

use crate::model::Schema;

/// Merge parent schema into child schema
pub fn merge_schemas(_parent: &Schema, _child: &mut Schema) {
    todo!("Implement schema inheritance merging")
}
