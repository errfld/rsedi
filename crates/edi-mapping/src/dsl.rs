//! Mapping DSL

/// Mapping DSL parser and representation
pub struct MappingDsl;

impl MappingDsl {
    /// Create a new mapping DSL instance
    pub fn new() -> Self {
        Self
    }
}

impl Default for MappingDsl {
    fn default() -> Self {
        Self::new()
    }
}
